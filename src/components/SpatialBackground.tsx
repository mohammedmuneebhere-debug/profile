import SpatialDepthField from './SpatialDepthField'

/** Fixed spatial depth layer — background only, no content parallax */
export default function SpatialBackground() {
  return <SpatialDepthField />
}
